#include "Linked_list.h"


int main(){
    Node* head = NULL;
    menu(head);
    string S = "  nguyen  VAN   aN   ";
    S = standardize(S);
    cout << S << '\n';
    // addEmp(head);
    return 0;
}
